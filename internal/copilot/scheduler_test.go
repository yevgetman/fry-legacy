package copilot

// TickScheduler tests removed — the TickScheduler type and BuildTickArgs
// utility were removed in the independent-heartbeat refactor. The copilot
// now manages its own schedule via CronCreate.
