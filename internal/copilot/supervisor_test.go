package copilot

// Supervisor tests removed — the Supervisor type was removed in the
// independent-heartbeat refactor. The copilot now manages its own
// schedule via CronCreate; fry main no longer supervises it.
